_configure() {
	convmv -f cp936 -t utf-8 -r --nosmart --notest .	
	patch -Np1 -i ${COMPDIR}/unzip/unzip-6.0-consolidated_fixes-1.patch
}

_build() {
	make -f unix/Makefile generic
}

_install() {
	make prefix=/usr MANDIR=/usr/share/man/man1 \
	 -f unix/Makefile install
}
